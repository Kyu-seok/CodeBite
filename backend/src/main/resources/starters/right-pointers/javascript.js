/**
 * Definition for a Node.
 */
class Node {
    constructor(val = 0, left = null, right = null, next = null) {
        this.val = val;
        this.left = left;
        this.right = right;
        this.next = next;
    }
}

class Solution {
    /**
     * @param {Node} root
     * @return {Node}
     */
    connect(root) {
        // Write your solution here
    }
}
