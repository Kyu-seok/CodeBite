from typing import Optional

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def captureDockSilhouette(self, root: Optional[TreeNode]) -> list[int]:
        # Write your solution here
        pass
