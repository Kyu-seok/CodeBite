from typing import Optional

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def assembleBreakdown(self, preorder: list[int], inorder: list[int]) -> Optional[TreeNode]:
        # Write your solution here
        pass
