/**
 * Definition for a Node.
 */
class Node {
    constructor(val = 0, next = null, random = null) {
        this.val = val;
        this.next = next;
        this.random = random;
    }
}

class Solution {
    /**
     * @param {Node} head
     * @return {Node}
     */
    duplicateTopicWeb(head) {
        // Write your solution here
    }
}
