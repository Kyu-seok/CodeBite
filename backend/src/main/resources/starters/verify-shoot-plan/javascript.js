class Solution {
    /**
     * @param {number} numScenes
     * @param {number[][]} prereqs
     * @return {boolean}
     */
    verifyShootPlan(numScenes, prereqs) {
        // Write your solution here
    }
}
