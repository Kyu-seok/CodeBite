#include <iostream>
using namespace std;

{USER_CODE}

int main() {
    int x, y;
    cin >> x >> y;
    int result = Solution().countFlagMismatches(x, y);
    cout << result << endl;
    return 0;
}
